{
  "dependencies": {
    "bootstrap": "^5.1.3",
    "bootstrap-icons": "^1.8.3",
    "json-server": "^0.17.0"
  },
  "name": "webapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "json-server --watch db/db.json --port 3001",
    "backfront": "json-server --watch db/db.json --port 3001 --static ./app"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
